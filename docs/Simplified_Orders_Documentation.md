# 📦 نظام الطلبات المبسط - Quick API
## دليل مطور تطبيقات الموبايل

---

## 📋 نظرة عامة

نظام Quick يدعم نوعين من الطلبات بنظام حالات موحد ومبسط:

| النوع | الوصف | المصدر |
|-------|-------|--------|
| **الطلب العادي (Order)** | منتجات من المتاجر المسجلة | Checkout |
| **الطلب الخاص (CustomOrder)** | اطلب أي شيء - وصف + موقع | إنشاء مباشر |

---

## 🔄 الحالات الأربعة المبسطة

```
    ┌─────────────────────────────────────────────────────────────┐
    │                    الحالات الأربعة                           │
    ├─────────────────────────────────────────────────────────────┤
    │                                                             │
    │   ┌──────────┐      ┌──────────┐      ┌──────────┐         │
    │   │  معلق    │ ───► │قيد التوصيل│ ───► │تم التسليم│         │
    │   │ pending  │      │ shipping │      │delivered │         │
    │   └────┬─────┘      └────┬─────┘      └──────────┘         │
    │        │                 │                                  │
    │        │                 │                                  │
    │        ▼                 ▼                                  │
    │   ┌────────────────────────────┐                           │
    │   │          ملغي               │                           │
    │   │       cancelled             │                           │
    │   │      (مع سبب)               │                           │
    │   └────────────────────────────┘                           │
    │                                                             │
    └─────────────────────────────────────────────────────────────┘
```

| الحالة | القيمة | الوصف بالعربية |
|--------|--------|----------------|
| **معلق** | `pending` | تم الطلب وبانتظار قبول سائق |
| **قيد التوصيل** | `shipping` | السائق قبل ويوصّل الطلب |
| **تم التسليم** | `delivered` | تم التسليم للزبون ✅ |
| **ملغي** | `cancelled` | ملغي أو فشل التسليم (مع سبب) |

---

## 📊 مسار الطلب

### للطلبين (Order & CustomOrder)

```
    👤 المستخدم                🖥️ الخادم                🚗 السائق
        │                          │                        │
        │  إنشاء الطلب             │                        │
        │  POST /orders            │                        │
        │  POST /custom-orders     │                        │
        │ ─────────────────────────>                        │
        │                          │                        │
        │  status: pending         │   📢 إشعار للسائقين    │
        │  ⏱️ 5 دقائق للقبول       │ ─────────────────────> │
        │<─────────────────────────│                        │
        │                          │                        │
        │                          │   قبول الطلب           │
        │                          │   POST /{id}/accept    │
        │                          │ <───────────────────── │
        │                          │                        │
        │  📱 السائق قبل طلبك      │   status: shipping     │
        │  status: shipping        │                        │
        │<─────────────────────────│                        │
        │                          │                        │
        │                          │   تسليم أو إلغاء       │
        │                          │   POST /{id}/deliver   │
        │                          │   POST /{id}/cancel    │
        │                          │ <───────────────────── │
        │                          │                        │
        │  📱 تم التوصيل / ملغي    │   status: delivered    │
        │  status: delivered       │       أو cancelled     │
        │<─────────────────────────│                        │
        │                          │                        │
        │  [إذا ملغي] إعادة محاولة │                        │
        │  POST /{id}/retry        │                        │
        │ ─────────────────────────>                        │
        │  status: pending         │                        │
        │                          │                        │
        └──────────────────────────┴────────────────────────┘
```

---

## ⏱️ آلية انتظار السائق

```
    ┌─────────────────────────────────────────────────────────────────────┐
    │                     تدفق انتظار السائق                              │
    └─────────────────────────────────────────────────────────────────────┘

    1️⃣ إنشاء الطلب
         │
         ▼
    ┌──────────────────┐
    │    pending       │  ← confirmation_expires_at = +5 دقائق
    │ is_available=true│
    └────────┬─────────┘
             │
       ┌─────┴─────┐
       │           │
       ▼           ▼
    ╔═══════╗   ╔══════════════════╗
    ║ سائق  ║   ║  انتهت الصلاحية   ║
    ║ قبِل  ║   ║ is_confirmation  ║
    ╚═══╤═══╝   ║ _expired = true  ║
        │       ╚═════════╤════════╝
        │                 │
        ▼                 ▼
    ┌────────┐   ┌──────────────────────┐
    │shipping│   │      pending         │
    │        │   │ can_resend = true    │
    └────────┘   │ زر "تأكيد الطلب" يظهر │
                 └──────────┬───────────┘
                            │
                            ▼ المستخدم يضغط /resend
                 ┌──────────────────┐
                 │    pending       │  ← expires_at جديد +5 دقائق
                 │ is_available=true│
                 └──────────────────┘
```

### الملخص:
- عند إنشاء الطلب: `pending` + `confirmation_expires_at = now + 5 دقائق`
- إذا انتهت الصلاحية بدون قبول سائق: **الطلب يبقى pending** ولكن `can_resend_to_drivers = true`
- المستخدم يضغط `/resend` لتجديد الصلاحية وإرسال إشعارات جديدة للسائقين
- **الطلب لا يُلغى تلقائياً** عند انتهاء الصلاحية

---

## 🔌 APIs Reference

### User APIs - المستخدم

#### الطلب العادي (Order)

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| `GET` | `/api/orders` | قائمة طلباتي |
| `GET` | `/api/orders/{id}` | تفاصيل طلب |
| `POST` | `/api/orders/{id}/cancel` | إلغاء الطلب (معلق فقط) |
| `POST` | `/api/orders/{id}/resend` | ⚡ إعادة إرسال للسائقين (تجديد الصلاحية) |
| `POST` | `/api/orders/{id}/retry-delivery` | إعادة محاولة بعد الإلغاء |

#### الطلب الخاص (CustomOrder)

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| `POST` | `/api/custom-orders/calculate-fee` | حساب سعر التوصيل |
| `POST` | `/api/custom-orders` | إنشاء طلب (معلق مباشرة) |
| `GET` | `/api/custom-orders` | قائمة طلباتي |
| `GET` | `/api/custom-orders/{id}` | تفاصيل طلب |
| `POST` | `/api/custom-orders/{id}/cancel` | إلغاء (معلق فقط) |
| `POST` | `/api/custom-orders/{id}/resend` | ⚡ إعادة إرسال للسائقين |
| `POST` | `/api/custom-orders/{id}/retry-delivery` | إعادة محاولة بعد الإلغاء |

### Driver APIs - السائق

#### الطلب العادي

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| `GET` | `/api/driver/orders/available` | الطلبات المتاحة |
| `GET` | `/api/driver/orders/my` | طلباتي |
| `POST` | `/api/driver/orders/{id}/accept` | قبول وبدء التوصيل |
| `POST` | `/api/driver/orders/{id}/deliver` | تأكيد التسليم |
| `POST` | `/api/driver/orders/{id}/cancel` | إلغاء التوصيل (مع سبب) |

#### الطلب الخاص

| الطريقة | المسار | الوصف |
|---------|--------|-------|
| `GET` | `/api/driver/custom-orders/available` | الطلبات المتاحة |
| `GET` | `/api/driver/custom-orders/my` | طلباتي |
| `POST` | `/api/driver/custom-orders/{id}/accept` | قبول وبدء التوصيل |
| `POST` | `/api/driver/custom-orders/{id}/deliver` | تأكيد التسليم |
| `POST` | `/api/driver/custom-orders/{id}/cancel` | إلغاء التوصيل (مع سبب) |

---

## 📝 Request/Response Examples

### إنشاء طلب خاص

**Request:**
```http
POST /api/custom-orders
Authorization: Bearer {user_token}
Content-Type: application/json

{
    "delivery_address": "حماة، شارع الوليد",
    "delivery_lat": 35.1318,
    "delivery_lng": 36.7514,
    "distance_km": 5.5,
    "is_immediate": true,
    "items": [
        {
            "description": "بيتزا عائلي",
            "pickup_address": "مطعم بيتزا هت",
            "pickup_lat": 35.1298,
            "pickup_lng": 36.7489
        }
    ]
}
```

**Response:**
```json
{
    "status": "success",
    "message": "تم إنشاء الطلب بنجاح",
    "data": {
        "id": 1,
        "status": "pending",
        "status_text": "معلق",
        "delivery_fee": 27500,
        "confirmation_expires_at": "2026-01-16T10:40:00+00:00",
        "is_confirmation_expired": false,
        "can_resend_to_drivers": false,
        "is_available_for_driver": true
    }
}
```

### إعادة إرسال للسائقين (بعد انتهاء الصلاحية)

**Request:**
```http
POST /api/orders/1/resend
Authorization: Bearer {user_token}
```

**Response:**
```json
{
    "status": "success",
    "message": "تم إعادة إرسال الطلب للسائقين",
    "data": {
        "id": 1,
        "status": "pending",
        "confirmation_expires_at": "2026-01-16T10:45:00+00:00",
        "is_confirmation_expired": false,
        "can_resend_to_drivers": false,
        "is_available_for_driver": true
    }
}
```

### إلغاء التوصيل (السائق)

**Request:**
```http
POST /api/driver/orders/1/cancel
Authorization: Bearer {driver_token}
Content-Type: application/json

{
    "reason": "العميل غير متواجد"
}
```

**Response:**
```json
{
    "status": "success",
    "message": "تم إلغاء التوصيل",
    "data": {
        "id": 1,
        "status": "cancelled",
        "status_text": "ملغي",
        "cancellationReason": "العميل غير متواجد"
    }
}
```

---

## 🔐 Authentication

```
Authorization: Bearer {token}
Accept: application/json
```

- **User APIs**: Guard `api`
- **Driver APIs**: Guard `driver`

---

## ⚠️ Error Handling

| الكود | الرسالة |
|-------|---------|
| `400` | لا يمكن إلغاء هذا الطلب في حالته الحالية |
| `400` | يجب إضافة عنصر واحد على الأقل |
| `400` | تم قبول هذا الطلب من سائق آخر |
| `400` | حسابك غير نشط حالياً |
| `400` | يجب تقديم سبب الإلغاء |
| `400` | هذا الطلب غير ملغي |
| `404` | الطلب غير موجود |

---

## 💰 حساب سعر التوصيل

```
delivery_fee = distance_km × km_price
```

- `km_price` من جدول `profit_ratios` (tag: `km_price`)
- `distance_km` يحسبها Frontend

---

**آخر تحديث:** 16 يناير 2026
